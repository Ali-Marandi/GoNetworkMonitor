# راهنمای جامع نصب و استفاده از GoNetworkMonitor Enterprise (نسخه ویندوز)

خوش آمدید! شما در حال استفاده از نسخه **Enterprise (v3.0.0)** نرم‌افزار GoNetworkMonitor هستید. این نسخه شامل قابلیت‌های پیشرفته‌ای نظیر تحلیل هوشمند ترافیک (AI)، ذخیره‌سازی دائمی در دیتابیس و خروجی متریک‌های Prometheus است.

---

## ۱. پیش‌نیاز الزامی: نصب درایور Npcap

ویندوز به تنهایی قادر به ضبط پکت‌های خام شبکه نیست. برای این کار حتماً باید درایور **Npcap** را نصب کنید.

1.  به سایت [Npcap.com](https://npcap.com/#download) مراجعه کنید.
2.  فایل **Npcap Installer** را دانلود و اجرا کنید.
3.  **نکته حیاتی:** در حین نصب، حتماً گزینه **"Install Npcap in WinPcap API-compatible Mode"** را فعال کنید. بدون این گزینه، برنامه قادر به شناسایی کارت شبکه نخواهد بود.

---

## ۲. دریافت و آماده‌سازی نرم‌افزار

1.  به بخش [Releases](https://github.com/Ali-Marandi/GoNetworkMonitor/releases/latest) در مخزن گیت‌هاب بروید.
2.  فایل **`gonetmon-v3.0.0-windows-amd64.zip`** را دانلود کنید.
3.  محتویات فایل ZIP را در یک پوشه ثابت (مثلاً `C:\GoNetMon`) استخراج کنید.

---

## ۳. نحوه اجرای صحیح (دسترسی مدیریت)

ضبط ترافیک شبکه یک عملیات حساس سیستمی است و نیاز به دسترسی سطح بالا دارد:

1.  روی فایل **`gonetmon-v3.0.0-windows-amd64.exe`** راست‌کلیک کنید.
2.  گزینه **Run as Administrator** را انتخاب کنید.
3.  اگر پنجره Windows Firewall باز شد، اجازه دسترسی (Allow Access) را صادر کنید.

---

## ۴. کار با داشبورد و قابلیت‌های Enterprise

پس از اجرا، پنجره کنسول باز باقی می‌ماند. حالا مراحل زیر را دنبال کنید:

*   **دسترسی به رابط کاربری:** مرورگر خود را باز کرده و به آدرس `http://localhost:8080` بروید.
*   **شروع ضبط:** از منوی بالا، کارت شبکه متصل به اینترنت خود را انتخاب کرده و روی **Start Capture** کلیک کنید.
*   **هوش مصنوعی (AI Intelligence):** از منوی سمت چپ به بخش AI بروید. در اینجا سیستم به صورت خودکار ناهنجاری‌ها و حملات احتمالی را شناسایی و گزارش می‌کند.
*   **دیتابیس:** تمام داده‌ها در فایل `network_monitor.db` کنار فایل اجرایی ذخیره می‌شوند. با بستن و باز کردن مجدد برنامه، تاریخچه داده‌ها حفظ خواهد شد.
*   **Prometheus:** اگر از Grafana استفاده می‌کنید، متریک‌ها در آدرس `http://localhost:8080/metrics` در دسترس هستند.

---

## ۵. عیب‌یابی سریع (Troubleshooting)

| مشکل | علت احتمالی | راه حل |
| :--- | :--- | :--- |
| لیست کارت‌های شبکه خالی است | عدم نصب Npcap یا عدم تیک زدن گزینه WinPcap Mode | Npcap را مجدداً نصب و گزینه مذکور را تیک بزنید. |
| خطای Permission Denied | عدم اجرا با دسترسی ادمین | روی فایل راست‌کلیک کرده و Run as Administrator را بزنید. |
| داشبورد باز نمی‌شود | پورت ۸۰۸۰ توسط برنامه دیگری اشغال شده | فایل `config.json` را ویرایش کرده و پورت را تغییر دهید (مثلاً ۹۰۹۰). |
| پکت‌ها نمایش داده نمی‌شوند | انتخاب کارت شبکه اشتباه | در منوی تنظیمات، کارت شبکه‌ای که دارای IP محلی (مثلاً 192.168.x.x) است را انتخاب کنید. |

---

**توسعه‌دهنده:** [Ali Marandi](https://github.com/Ali-Marandi)
**نسخه:** 3.0.0 Enterprise Edition
