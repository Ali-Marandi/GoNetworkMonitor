# راهنمای جامع نصب و استفاده از GoNetworkMonitor در ویندوز

این راهنما به شما کمک می‌کند تا نرم‌افزار **GoNetworkMonitor v2.0.0** را به درستی روی سیستم‌عامل ویندوز نصب و راه‌اندازی کنید.

---

## ۱. پیش‌نیاز حیاتی: نصب Npcap

از آنجایی که ویندوز به صورت پیش‌فرض ابزاری برای ضبط مستقیم پکت‌های خام شبکه ندارد، نصب درایور **Npcap** الزامی است.

1.  به وب‌سایت رسمی [Npcap.com](https://npcap.com/#download) بروید.
2.  آخرین نسخه **Npcap Installer** را دانلود و اجرا کنید.
3.  **بسیار مهم:** در طول مراحل نصب، حتماً گزینه زیر را تیک بزنید:
    > *"Install Npcap in WinPcap API-compatible Mode"*
4.  نصب را به پایان برسانید و در صورت نیاز سیستم را ری‌استارت کنید.

---

## ۲. دانلود و استخراج نرم‌افزار

1.  به صفحه [Releases در GitHub](https://github.com/Ali-Marandi/GoNetworkMonitor/releases) بروید.
2.  فایل **`gonetmon-v2.0.0-windows-amd64.zip`** را دانلود کنید.
3.  فایل ZIP را در یک پوشه دلخواه (مثلاً `C:\GoNetMon`) استخراج (Extract) کنید.

---

## ۳. نحوه اجرای برنامه

برای اینکه برنامه بتواند به کارت شبکه شما دسترسی پیدا کند، باید با دسترسی مدیریت اجرا شود:

1.  روی فایل **`gonetmon-v2.0.0-windows-amd64.exe`** کلیک راست کنید.
2.  گزینه **Run as Administrator** را انتخاب کنید.
3.  در صورت ظاهر شدن پنجره Windows Firewall، دکمه **Allow Access** را بزنید تا اجازه دسترسی به شبکه داده شود.

---

## ۴. استفاده از داشبورد وب

پس از اجرای موفق، یک پنجره مشکی (Console) باز می‌شود که نشان می‌دهد سرور فعال شده است.

1.  مرورگر خود (Chrome, Firefox, Edge) را باز کنید.
2.  آدرس زیر را در نوار آدرس وارد کنید:
    `http://localhost:8080`
3.  اکنون داشبورد مدرن GoNetworkMonitor را مشاهده می‌کنید.
4.  از منوی بالا سمت راست، کارت شبکه (Interface) مورد نظر خود را انتخاب کرده و دکمه **Start Capture** را بزنید.

---

## ۵. تنظیمات و سفارشی‌سازی

برنامه از فایل `config.json` برای ذخیره تنظیمات استفاده می‌کند. اگر این فایل وجود نداشته باشد، برنامه از مقادیر پیش‌فرض استفاده می‌کند.

*   **تغییر پورت:** اگر پورت ۸۰۸۰ توسط برنامه دیگری اشغال شده است، می‌توانید فایل `config.json` را با Notepad باز کرده و مقدار `"port"` را تغییر دهید.
*   **هشدارها (Alerts):** می‌توانید آستانه مصرف پهنای باند را در بخش Settings داشبورد تنظیم کنید تا در صورت مصرف بیش از حد، هشدار دریافت کنید.

---

## ۶. عیب‌یابی (Troubleshooting)

| مشکل | راه حل |
| :--- | :--- |
| **خطای "No interfaces found"** | مطمئن شوید Npcap نصب شده و برنامه را با Run as Administrator اجرا کرده‌اید. |
| **داشبورد باز نمی‌شود** | بررسی کنید که آنتی‌ویروس یا فایروال پورت ۸۰۸۰ را مسدود نکرده باشد. |
| **عدم نمایش پکت‌ها** | کارت شبکه صحیح را در داشبورد انتخاب کنید (معمولاً گزینه‌ای که دارای IP محلی شماست). |

---

**توسعه داده شده توسط:** [Ali Marandi](https://github.com/Ali-Marandi)
**نسخه:** 2.0.0 (Commercial Edition)
